/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export'  // Commented out to resolve the image optimization issue
  // You can add other configurations here if needed
}

module.exports = nextConfig;
